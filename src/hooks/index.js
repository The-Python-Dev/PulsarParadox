/**
 * src/hooks/index.js
 *
 * Custom hooks — exported here as they're built.
 *
 * PHASE 2 hooks to add:
 * - useScrollPosition      (for navbar scroll state)
 * - useReducedMotion       (wraps prefers-reduced-motion)
 * - useCanRenderParticles  (performance detection for Three.js)
 * - useLenis               (Lenis smooth scroll instance)
 * - useGSAP                (GSAP context wrapper)
 */

// Placeholder — hooks added in Phase 2+
export {};
